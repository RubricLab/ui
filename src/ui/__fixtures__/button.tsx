import React from 'react';
import { Button } from '../button';

const buttonFixtures = {
  large: <Button size="lg" disabled={false}>Button</Button>,
  medium: <Button size="md" disabled={false}>Button</Button>,
  small: <Button size="sm" disabled={false}>Button</Button>,
};

export default buttonFixtures;
