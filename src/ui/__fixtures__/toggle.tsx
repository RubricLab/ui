import React from "react";
import { Toggle } from "../toggle";

const toggleFixtures = {
	sm: <Toggle size="sm">R</Toggle>,
	md: <Toggle size="md">R</Toggle>,
	lg: <Toggle size="lg">R</Toggle>,
};

export default toggleFixtures;
